# Example environment variables for live LDAPS testing
# Copy this file to `.env` (or export these in your shell) and
# adjust values for your environment. Do NOT commit secrets.

# Enable the optional live test (skipped by default)
LDAP_LIVE=0

# Option A: Provide a full LDAP(S) URL
#LDAP_URL=ldaps://ldap.example.com:636

# Option B: Provide individual parts
#LDAP_HOST=ldap.example.com
#LDAP_PORT=636
#LDAP_PROTOCOL=ldaps   # ldap | ldaps

# Base DN to search (required when LDAP_LIVE=1)
LDAP_BASE=dc=example,dc=com

# Optional bind credentials
#LDAP_BIND_DN=cn=admin,dc=example,dc=com
#LDAP_BIND_PW=super-secret

# Optional search filter (defaults to (objectClass=*))
#LDAP_FILTER=(uid=alice)

# TLS settings (for ldaps)
# Set to 1 to disable TLS verification (insecure; for testing only)
LDAP_TLS_INSECURE=0

# Provide a CA bundle as a file path or inline PEM
#LDAP_CA_PATH=/absolute/path/to/ca-bundle.pem
#LDAP_CA_PEM=-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----

